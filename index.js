const rename = require('./src/rename')
const remeta = require('./src/remeta')
const WinRAR = require('./src/winrar')
const recompose = require('./src/recompose')
module.exports = {
  rename,
  remeta,
  WinRAR,
  recompose,
}